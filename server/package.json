{
  "name": "CRIMP-server",
  "description": "Backend of CRIMP system",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "body-parser": "1.x",
    "express": "4.x",
    "pg": "2.x"
  },
  "scripts": {
	  "start": "node CRIMP.js",
	  "test": "echo \"Error: no test specified\" && exit 1"
	}
}